{
  "framework": "nextjs",
  "buildCommand": "DATABASE_URL=$DATABASE_URL_UNPOOLED npx prisma db push --accept-data-loss 2>/dev/null; npx prisma generate && next build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "env": {
    "NEXT_PUBLIC_SITE_URL": "https://sandak-store.vercel.app"
  }
}
