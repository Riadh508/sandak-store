{
  "framework": "nextjs",
  "buildCommand": "DATABASE_URL=$DATABASE_URL_UNPOOLED npx prisma migrate resolve --applied 20260525000000_init 2>/dev/null; DATABASE_URL=$DATABASE_URL_UNPOOLED npx prisma migrate deploy && npx prisma generate && next build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "env": {
    "PRISMA_CLIENT_ENGINE_TYPE": "dataproxy",
    "NEXT_PUBLIC_SITE_URL": "https://sandak-store.vercel.app"
  }
}
