{
  "framework": "nextjs",
  "buildCommand": "git archive --format=zip HEAD -o public/downloads/sandak-store-v4.zip 2>/dev/null; npx prisma db push --accept-data-loss 2>/dev/null; npx prisma generate && next build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}
